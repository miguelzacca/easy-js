{
  "name": "easy",
  "version": "1.0.0",
  "description": "Scripts e css prontos para uso,  simplificando o desenvolvimento e removendo a nescessidade de criar manualmente a logica em javascript e estilização com css. - Você só precisara do html no frontend!",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && terser lib/*.js --compress --mangle --mangle-props --toplevel --output lib/easy-form.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "terser": "^5.33.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
